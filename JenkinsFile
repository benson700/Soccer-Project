pipeline {
    agent any
    stages{
        stage('test') {
            steps{
                sh "python3 tests/test_routes.py"
            }
        }
    }
        post {
           always {
               archiveArtifacts artifacts:"Jsoncov/*"
           }
    }
}